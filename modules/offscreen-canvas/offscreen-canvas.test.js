import test from "ava";
import OffscreenCanvas from "./offscreen-canvas";

test.beforeEach((t) => {
    t.context = new OffscreenCanvas(100, 200);
});

test.todo("constructor");

test.todo("setSize");

test.todo("setSize");

test.todo("render");

test.todo("clear");

test.todo("get and set imageData");

test.todo("toImg");
